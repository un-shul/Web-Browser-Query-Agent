# Virtualenvs
venv/
.venv/
__pycache__/
*.pyc

# Secrets -- never commit these
.env
.env.local

# Large local model artifacts (regenerate with train_classifier.py)
embedding_model/
*.safetensors
*.h5
*.pt
*.ckpt

# Local vector store
chroma_db/

# The sklearn pickle is a convenience artifact; classifier.json is the one
# that matters and is committed deliberately (see the negation below).
*.pkl
!classifier.json

.embed_cache.json
*.log
.DS_Store
.vercel
