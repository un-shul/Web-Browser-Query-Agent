# Core web app
flask==3.1.2
requests==2.32.5
beautifulsoup4==4.14.2
python-dotenv==1.1.1

# Semantic cache
chromadb==1.3.0

# Local model stack. Only needed for EMBED_PROVIDER=local / SUMMARIZER=local.
# Excluded from the serverless bundle -- see vercel.json.
sentence-transformers==5.1.2
transformers==4.57.1
scikit-learn==1.7.2
pandas==2.3.3
