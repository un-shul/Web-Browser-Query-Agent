# Production dependencies. Deliberately small: Vercel's Python bundle limit is
# 500MB uncompressed and torch alone is 547MB, so the deployed app uses hosted
# APIs for embeddings, inference and the vector store.
#
# For the local model stack (EMBED_PROVIDER=local, SUMMARIZER=local,
# VECTOR_STORE=chroma) install requirements-local.txt as well.
flask==3.1.2
requests==2.32.5
beautifulsoup4==4.14.2
python-dotenv==1.1.1
