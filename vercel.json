{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "app.py": {
      "maxDuration": 120,
      "excludeFiles": "{tests/**,docs/**,*.csv,*.md,embedding_model/**,chroma_db/**,*.pkl,requirements-local.txt,requirements-dev.txt,.venv/**,*.png}"
    }
  }
}
