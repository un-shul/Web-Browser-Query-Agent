-r requirements-local.txt
-r requirements-test.txt

# Headless-browser fallback for JS-heavy pages, and for driving the UI while
# working on it. Local only; cannot run on Vercel serverless.
# After installing: python -m playwright install chromium
playwright==1.56.0
